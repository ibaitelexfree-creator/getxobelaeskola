# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT VARIABLES
# -----------------------------------------------------------------------------
# Rename this file to .env.production.local or add these to your Vercel Project Settings.

# --- SUPABASE CONFIGURATION ---
# Found in Supabase Dashboard -> Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-public-anon-key

# CRITICAL: Keep this secret! Never expose to client-side.
# Used for Admin API routes (e.g. generating certificates, verifying logic)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- APP CONFIGURATION ---
# The absolute URL of your production deployment (e.g. https://getxobela.com)
# Used for Stripe callbacks, email links, etc.
NEXT_PUBLIC_APP_URL=https://your-production-domain.com

# --- STRIPE CONFIGURATION ---
# Found in Stripe Dashboard -> Developers -> API Keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_SECRET_KEY=sk_live_...

# Found in Stripe Dashboard -> Developers -> Webhooks
# You must create a webhook endpoint for: https://your-domain.com/api/webhook
STRIPE_WEBHOOK_SECRET=whsec_...
